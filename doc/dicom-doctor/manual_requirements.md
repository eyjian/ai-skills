目标：输入一个dicom文件或者zip压缩包，驱动AI阅片，重点检查是否存在结节等异常，输出一份pdf检视报告。

需要构建一个给openclaw等工具使用的dicom文件无损转png图片的工具，优先考虑使用DCMTK将dicom文件转换为png图片，如果不可用，其次可考虑SimpleITK和dicom2jpg。另外还可选择是否应用Real-ESRGAN超分提升。输入为dicom文件或者zip压缩包，输出为png图片或者超分后的png图片。让ai检视每张图片，重点检查是否存在结节等异常，租后中间给出报告，并指出异常存在哪些文件中，以方便找出来找医生看。最终输出一份pdf格式的报告。
